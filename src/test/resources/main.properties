# main.properties
project.date=${timestamp}
project.dir=${project.basedir}
#project.dir.screenshot=${project.basedir}/target/screenshots/
project.dir.surefire=${project.basedir}/target/${timestamp}/surefire-reports/
project.dir.output=${project.basedir}/target/${timestamp}/classes/
project.dir.testOutput=${project.basedir}/target/${timestamp}/test-output/
project.dir.allure.results=${project.basedir}/target/${timestamp}/allure/result/
project.dir.allure.report=${project.basedir}/target/${timestamp}/allure/report/

# Command to run "mvn clean test site" and not "mvn clean -Dtest=TestLogs test allure:report"